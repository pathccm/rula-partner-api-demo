{
  "name": "rula-partner-api-demo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint-fix": "turbo lint-fix",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format": "biome format --write .",
    "generate:client": "pnpm --filter @partner-scheduling-demo/api-client generate && biome format --write packages/api-client/src/generated.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "turbo": "^2.9.14",
    "typescript": "~6.0.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}

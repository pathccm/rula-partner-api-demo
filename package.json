{
  "name": "partner-scheduling-api-demo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint-fix": "turbo lint-fix",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format": "biome format --write .",
    "generate:client": "pnpm --filter @partner-scheduling-demo/api-client generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "turbo": "^2.5.4",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
